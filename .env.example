DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=magang_unpra

# Generate with: openssl rand -base64 32
JWT_SECRET=change_this_to_a_random_secret_key

PORT=8080

# Comma-separated: http://localhost:5173,https://yourdomain.com
CORS_ORIGINS=http://localhost:5173

# debug | release | test
GIN_MODE=release
